source "https://rubygems.org"

# Windows 파일 변경 감지
gem 'wdm', '>= 0.1.0' if Gem.win_platform?

# Jekyll 플러그인
group :jekyll_plugins do
  gem 'jekyll-feed'
  gem 'jekyll-sitemap'
  gem 'jekyll-seo-tag'
  gem 'jekyll-paginate-v2'   # ✅ v2로 전환
  # gem 'jekyll-paginate'    # ❌ 사용 안 함
  # gem 'jekyll-archives'    # ❌ autopages로 대체
end

# 마크다운/하이라이트
gem 'kramdown'
gem 'rouge'
